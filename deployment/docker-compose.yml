version: "3.9"

services:

  litellm:
    image: ghcr.io/berriai/litellm:main
    container_name: ai_gateway
    ports:
      - "4000:4000"

  vector_db:
    image: qdrant/qdrant
    container_name: ai_memory
    ports:
      - "6333:6333"
