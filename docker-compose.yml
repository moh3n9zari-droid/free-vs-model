version: "3.9"

services:

  gateway:
    build: .
    container_name: free_vs_model_gateway
    ports:
      - "8000:8000"
    env_file:
      - .env


  qdrant:
    image: qdrant/qdrant
    container_name: free_vs_model_memory
    ports:
      - "6333:6333"
